## Vue 3 前端实习生（30 分钟，无编码）面试题

### 面试目标与结构（30 分钟，无编码）
- **目标**: 快速评估候选人是否能在 Vue 3 项目中即插即用，理解核心概念与常见工程实践。
- **结构**: 3 个模块（核心 → 工程 → 质量/性能），每题 1-2 分钟，必要时追问。

### 模块一：Vue 3 核心（约 12 分钟）
1) **响应式原理与 API**
- **问题**: `ref` vs `reactive` 区别？何时会丢失响应式？为何使用 Proxy？
- **期望要点**: 基础类型用 `ref`，对象用 `reactive`；解包规则；解构/闭包/跨组件传递导致丢失；`toRef`/`toRefs`。

2) **computed / watch / watchEffect 场景**
- **问题**: 三者何时用？如何避免多余触发或循环依赖？
- **期望要点**: computed 纯派生、缓存；watch 观察特定源与副作用；watchEffect 即时依赖收集；清理副作用。

3) **组件通信与边界**
- **问题**: 父子通信（props/emits）、跨层（provide/inject）与全局状态分别适用场景？
- **期望要点**: 单向数据流；解耦 vs 可维护性；事件命名；避免滥用全局状态。

4) **v-model 机制**
- **问题**: 自定义组件如何支持 `v-model`？如何处理多个 `v-model` 和修饰符？
- **期望要点**: `modelValue`/`update:modelValue`；多模型命名；修饰符透传与处理。

5) **生命周期与数据获取**
- **问题**: 异步请求放在哪个生命周期？SSR/同构时怎么处理？
- **期望要点**: `onMounted` 客户端请求；`onBeforeUnmount` 清理；SSR 避免直接访问 window。

6) **列表渲染与 key**
- **问题**: 为什么必须提供稳定的 `key`？使用索引的风险？
- **期望要点**: Diff 与复用；状态错位问题。

### 模块二：工程与日常开发（约 10 分钟）
7) **路由（Vue Router 4）**
- **问题**: 动态路由、导航守卫、懒加载、滚动行为如何配置？
- **期望要点**: 懒加载分包；`beforeEach`/路由元信息；`scrollBehavior`。

8) **状态管理（Pinia）**
- **问题**: 为什么选 Pinia？如何组织 store、持久化与类型推断？
- **期望要点**: 模块化；`setup`/`option` store；持久化插件；类型安全的 `defineStore`。

9) **HTTP 请求与封装**
- **问题**: 如何封装请求层（baseURL、拦截器、错误处理、取消/重试/去抖）？
- **期望要点**: 统一 loading/error；幂等/重试退避；中止控制；鉴权与刷新 token。

10) **TypeScript 实践**
- **问题**: `defineProps`/`defineEmits` 类型标注；接口返回类型断言如何做安全？
- **期望要点**: 泛型/工具类型；最小可信输入；运行时校验（如 zod）。

11) **Vite 与环境管理**
- **问题**: 环境变量、代理、本地/预发配置与构建体积优化如何落实？
- **期望要点**: `import.meta.env`；代理跨域；分包/按需引入；分析工具。

### 模块三：质量、性能与可维护（约 8 分钟）
12) **性能优化**
- **问题**: 避免不必要渲染的手段？长列表与频繁交互的优化？
- **期望要点**: 合理拆分组件；computed 缓存；虚拟滚动；节流/防抖；`keep-alive`/`suspense`。

13) **表单与校验**
- **问题**: 异步校验和统一错误提示如何设计？
- **期望要点**: 校验库选择；失焦/输入时机；错误态与禁用态一致性。

14) **组件复用与约定**
- **问题**: 设计一个可复用表单项需要考虑哪些点？
- **期望要点**: 受控/非受控；`props`/`emits` 契约；`slots` 与 attrs 透传；样式隔离。

15) **错误处理与空态**
- **问题**: 全局错误边界/接口异常/重试与空态占位如何统一？
- **期望要点**: 全局捕获与上报；可重试与兜底；一致的空态组件。

16) **安全与可访问性**
- **问题**: `v-html` 风险？基本 a11y 实践？
- **期望要点**: XSS 来源与转义；表单语义/焦点管理/键盘可达。

### 追问示例（任选 2）
- **为什么 watch 某对象字段时不触发？**（浅/深侦听、`flush`、依赖来源）
- **Pinia 中如何跨标签页同步登录态？**（storage 事件/广播通道/持久化）

### 评分建议（参考）
- **核心概念 40 分**: 响应式、computed/watch、通信、v-model、生命周期、key。
- **工程实践 35 分**: 路由、Pinia、请求封装、TS、Vite。
- **质量与性能 25 分**: 性能、表单、组件设计、错误/空态、安全/a11y。
- **通过线**: 总分 ≥ 60 且核心部分 ≥ 24。加分关注：能给出现实项目场景与取舍理由。

### 红旗信号
- 将 Vue 2 API 与 Vue 3 混淆；对 `ref`/`reactive` 与解包机制不清楚。
- 大量使用全局事件总线替代规范通信；忽视 key 或使用索引。
- 请求层无统一错误与取消策略；滥用 `v-html` 不知其风险。

