## 外包前端（中级偏上）完整面试题（5 套，每套约 30 分钟，含 Vue 3）

### 套题一｜Vue 3 核心与工程基础
1) Vue 3 响应式体系：简述基于 Proxy 的依赖收集与触发流程，`ref`/`reactive` 的适用差异与解包规则，列举 3 个会丢失响应式的常见场景并给出修复手法（如解构、闭包持有原值、跨组件传递）；再说明 `shallowRef/shallowReactive`、`markRaw/readonly` 的应用边界与典型使用案例；深入追问：数组与 Map/Set 的响应式差异如何处理？何时应以 `shallowRef` 包裹大型不可变对象以降低追踪开销？
2) computed / watch / watchEffect：给出一个依赖链较长的业务派生数据，说明应如何用 computed 组织，并阐述 watch 的侦听源选择、`deep/immediate/flush` 的取舍与避免回环的策略；对 `watchEffect` 的依赖收集与清理机制进行说明；深入追问：如何定位一次变更引发的级联多次触发？在高频输入场景如何用节流/去抖与 `flush: post` 协同减少重新渲染？
3) 自定义组件的 v-model 设计：说明单模型 `modelValue`/`update:modelValue` 的契约与多模型命名方式，如何透传和解析修饰符（如 `trim/number`），以及受控/非受控模式切换与一致性处理；深入追问：同一组件同时支持 `value` 与 `checked` 两种模型时，如何避免事件冲突与状态撕裂？IME 输入法下的变更时机如何处理？
4) Vue Router 4：描述动态路由与路由守卫的组织方式，路由级懒加载、数据预取、错误边界与滚动还原的配置要点，并说明并发导航下的竞态处理；深入追问：在 SSR 中如何与路由数据预取协同避免二次请求与水合不匹配？
5) 性能优化实务：场景包含长列表、频繁交互与多标签页切换，说明使用 `<KeepAlive>`/`Suspense`/`Teleport` 的边界与收益，长列表虚拟化策略，避免不必要渲染的手段（组件拆分、稳定 key、缓存派生）；深入追问：如何用 Performance/Profiler 定位渲染热点并量化改进前后指标（LCP/INP/CLS）？
6) 请求层与错误治理：设计统一请求封装（baseURL、拦截器、超时/取消、重试与退避、去重），令鉴权与刷新 Token 并发安全；统一错误码到用户可理解的提示并支持重试与兜底；深入追问：如何在乐观更新与失败回滚间保证幂等？如何与全局错误边界协同避免重复上报？

### 套题二｜状态管理与复杂表单
1) Pinia 架构与持久化：说明如何划分模块、区分 state/getters/actions、类型推断与复用；描述登录态/设置类状态的持久化、跨标签页同步与版本迁移策略；深入追问：存储冲突（多标签页同时修改）如何合并？SSR 场景如何隔离请求作用域避免状态泄漏？
2) 复杂表单设计：含多个字段联动与异步唯一性校验，阐述字段依赖建模、校验触发时机（输入/失焦/提交）、取消与去抖、统一错误提示与禁用态策略；深入追问：如何抽象通用表单项组件并通过 `v-model`、`slots` 与 attrs 透传兼顾可用性与复用性？
3) 上传与并发控制：大文件分片上传、并发数限制、失败重试与断点续传，进度与可取消；深入追问：如何设计幂等键以避免重复合并？与网络抖动/页面可见性变化协同时的状态一致性如何保证？
4) 列表与表格性能：10 万行级数据的展示、排序/筛选、冻结列与滚动同步，窗口化渲染与可变行高测量；深入追问：如何在虚拟化下实现行内编辑与选中态的稳定性？
5) 错误、空态与重试：统一处理接口异常、断网、超时，提供可重试与离线提示，Skeleton/空态与占位策略；深入追问：在轮询或实时场景如何避免叠加重试风暴与异常雪崩？
6) 设计系统与主题：Design Token、暗色模式、CSS 变量与按需引入，组件库二次封装与无障碍基线；深入追问：主题切换对动态渲染性能与可维护性的影响如何评估与回归测试？

### 套题三｜网络、安全与交付
1) CORS 与身份：解释预检、凭证、`SameSite` 与跨域 Cookie 限制，前端常见鉴权头携带策略与风险；深入追问：在需要携带 Cookie 的跨域场景下，如何与后端协同配置 `Access-Control-*` 头避免安全隐患与功能失败？
2) OAuth2/OIDC（PKCE）：描述授权码 + PKCE 流程、Token 存储与刷新、并发刷新保护与登出/撤销；深入追问：前端如何在弱网下保证 Token 生命周期管理的正确性并最小化交互中断？
3) HTTP 缓存与回滚：静态资源长缓存（`contenthash+immutable`）、HTML 短缓存/协商缓存、版本清单与 CDN 回滚；深入追问：一次紧急回退如何保证入口与资源版本一致性且用户端最少刷新？
4) 资源优化：图片（AVIF/WebP、自适应、多尺寸）与字体（子集化、`font-display`、预加载）；深入追问：如何为关键图片设置优先级并评估对 LCP 的影响？
5) 监控与可观测：RUM 采集（LCP/INP/CLS）、错误上报与去重、Source Map 管理、用户隐私与采样；深入追问：如何基于监控数据建立性能预算并在 CI 中自动守护？
6) WebSocket 稳定性：断线重连（指数退避+抖动）、心跳/超时、消息 ack/序列号/去重缓存、离线队列；深入追问：如何在弱网导致乱序/重复的情况下保证端到端幂等与一致性？

### 套题四｜SSR/SSG 与大规模界面
1) 渲染模式选型：对营销页、后台、内容站点分别选择 SSR/SSG/ISR/纯 SPA 的理由与权衡（TTFB/TTI、缓存、运维成本）；深入追问：边缘节点缓存与数据回源策略如何设计以兼顾新鲜度与成本？
2) 水合不匹配定位：阐述非确定性渲染来源（时间、随机数、异步）、`onServerPrefetch`、差异定位手段与兜底方案；深入追问：如何在仅生产环境复现与排查这类问题并建立回归保护？
3) 流式与岛架构：解释流式 SSR 的优势与限制，岛架构的局部水合与边界条件；深入追问：对交互岛的依赖管理与数据流设计应如何避免水合后重复拉取？
4) 长列表与看板：大规模卡片/表格/图表混排的窗口化、测量、粘性列与滚动同步；深入追问：如何在虚拟化下保持拖拽/选择等交互的稳定与可达性（a11y）？
5) 构建与拆包：动态导入边界、共享依赖抽离、预构建与缓存、包体积可视化；深入追问：如何量化拆包对缓存命中率的影响并选择最优边界？
6) 微前端/Monorepo：模块联邦/iframe/路由拼装的取舍、样式隔离与依赖共享、发布与回滚；深入追问：如何在保证运行时稳定的同时控制工程复杂度与治理成本？

### 套题五｜性能攻坚与实战场景
1) 两周减半包体积：既有首屏 JS 3MB，目标两周降至 1.5MB，给出审计→落地→复测的步骤（依赖替换、按需引入、拆包、移除未用代码）；深入追问：如何避免“拆包过度”导致首屏请求过多与缓存命中率下降？
2) 低端机卡顿：描述定位 CPU/内存热点的方法（Performance/Profiler/内存快照），提出任务切片、虚拟化、延迟加载方案；深入追问：如何定义与验证“可接受的卡顿阈值”，以及在加载与交互间做权衡？
3) FCP/TTFB 异常：在 SSR/反向代理/上游依赖混合场景下，分析 TTFB 偏高的归因与优化（缓存、慢查询、边缘渲染）；深入追问：如何设计灰度实验验证改动对用户实际体验的收益？
4) 实时数据表格：大表格实时刷新且允许编辑，如何保证状态一致与可撤销；深入追问：在频繁更新与用户编辑冲突时，如何设计冲突解决与提示？
5) 安全与 `v-html`：说明 XSS 的来源与防护（模板转义、CSP、白名单），在必须使用 `v-html` 的场景如何最小化风险；深入追问：如何在第三方内容与富文本中做运行时净化并评估代价？
6) Server State vs Client State：明确边界、失效策略、并发与重试、去重；深入追问：在离线/弱网与切换页面时，如何既保证一致性又兼顾响应性？

