# 平台域名变更技术整改指引

## 一、背景说明

根据业主要求，平台域名需要从原域名 `https://fzh.ywtg.foshan.gov.cn` 调整为新域名 `https://yzhcs.fszsj.fs.gdgov.cn`。

所有平台应用均通过腾讯里约网关代理，保持同域访问。本次域名变更主要影响第三方对接应用和专题页面，需要各合作商配合完成相关技术改造。

## 二、域名变更影响范围

### 2.1 平台内部系统
- ✅ 已使用相对路径，可无缝切换
- ✅ 无需额外改造

### 2.2 第三方应用系统
- ⚠️ 前端登录SDK的loginUrl参数可能写死了旧域名
- ⚠️ 前端资源请求（JS/CSS/图片等）可能写死了旧域名
- ⚠️ 前端API请求路径可能写死了旧域名
- ⚠️ 后端调用平台Auth服务可能写死了旧域名

## 三、技术整改要求

### 3.1 核心原则
**所有资源和接口必须使用相对路径或动态获取当前域名，禁止硬编码域名地址。**

### 3.2 前端整改指引（Vue）

#### 3.2.1 登录SDK配置整改

**❌ 错误示例：**
```javascript
// 硬编码了旧域名
const loginConfig = {
  loginUrl: 'https://fzh.ywtg.foshan.gov.cn/auth/login',
  appId: 'your-app-id'
}
```

**✅ 正确示例：**
```javascript
// 方案1：使用相对路径
const loginConfig = {
  loginUrl: '/auth/login',
  appId: 'your-app-id'
}

// 方案2：动态获取当前域名
const loginConfig = {
  loginUrl: `${window.location.origin}/auth/login`,
  appId: 'your-app-id'
}
```

#### 3.2.2 API请求配置整改

**❌ 错误示例：**
```javascript
// axios配置硬编码域名
axios.defaults.baseURL = 'https://fzh.ywtg.foshan.gov.cn/api'

// 或直接在请求中写死域名
axios.get('https://fzh.ywtg.foshan.gov.cn/api/user/info')
```

**✅ 正确示例：**
```javascript
// 方案1：使用相对路径作为baseURL
axios.defaults.baseURL = '/api'

// 方案2：动态获取域名
axios.defaults.baseURL = `${window.location.origin}/api`

// 请求时使用相对路径
axios.get('/user/info')
```

#### 3.2.3 静态资源引用整改

**❌ 错误示例：**
```vue
<template>
  <img src="https://fzh.ywtg.foshan.gov.cn/static/logo.png" />
  <script src="https://fzh.ywtg.foshan.gov.cn/lib/sdk.js"></script>
</template>
```

**✅ 正确示例：**
```vue
<template>
  <!-- 使用相对路径 -->
  <img src="/static/logo.png" />
  <script src="/lib/sdk.js"></script>
  
  <!-- 或使用require/import -->
  <img :src="require('@/assets/logo.png')" />
</template>
```

#### 3.2.4 页面跳转整改

**❌ 错误示例：**
```javascript
window.location.href = 'https://fzh.ywtg.foshan.gov.cn/page/detail'
```

**✅ 正确示例：**
```javascript
// Vue Router跳转
this.$router.push('/page/detail')

// 或使用相对路径
window.location.href = '/page/detail'
```

### 3.3 后端整改指引（Java）

#### 3.3.1 调用平台Auth服务整改

**❌ 错误示例：**
```java
// 硬编码域名
public class AuthService {
    private static final String AUTH_URL = "https://fzh.ywtg.foshan.gov.cn/auth/getUserInfo";
    
    public UserInfo getUserInfo(String token) {
        RestTemplate restTemplate = new RestTemplate();
        return restTemplate.getForObject(AUTH_URL + "?token=" + token, UserInfo.class);
    }
}
```

**✅ 正确示例：**
```java
// 方案1：配置文件管理（推荐）
@Configuration
public class PlatformConfig {
    @Value("${platform.domain:}")
    private String platformDomain;
    
    @Value("${platform.auth.path:/auth/getUserInfo}")
    private String authPath;
    
    public String getAuthUrl() {
        return platformDomain + authPath;
    }
}

// application.yml
// platform:
//   domain: https://yzhcs.fszsj.fs.gdgov.cn
//   auth:
//     path: /auth/getUserInfo

// 方案2：从请求头获取域名
public UserInfo getUserInfo(String token, HttpServletRequest request) {
    String domain = request.getScheme() + "://" + request.getServerName();
    String authUrl = domain + "/auth/getUserInfo";
    RestTemplate restTemplate = new RestTemplate();
    return restTemplate.getForObject(authUrl + "?token=" + token, UserInfo.class);
}
```

#### 3.3.2 回调地址配置整改

**❌ 错误示例：**
```java
String callbackUrl = "https://fzh.ywtg.foshan.gov.cn/callback";
```

**✅ 正确示例：**
```java
@Value("${app.callback.url}")
private String callbackUrl;

// application.yml中配置
// app:
//   callback:
//     url: https://yzhcs.fszsj.fs.gdgov.cn/callback
```

## 四、整改验证清单

### 4.1 前端验证项
- [ ] 登录SDK的loginUrl参数未硬编码域名
- [ ] axios或其他HTTP客户端的baseURL未硬编码域名
- [ ] 所有静态资源引用使用相对路径
- [ ] 页面跳转链接未硬编码域名
- [ ] WebSocket连接地址未硬编码域名（如有）
- [ ] iframe嵌套地址未硬编码域名（如有）

### 4.2 后端验证项
- [ ] 调用平台服务的URL已配置化
- [ ] 回调地址已配置化
- [ ] 日志中不包含旧域名硬编码
- [ ] 配置文件已更新为新域名

### 4.3 测试验证
- [ ] 在新域名下能正常登录
- [ ] 在新域名下能正常获取用户信息
- [ ] 在新域名下所有功能正常运行
- [ ] 浏览器控制台无跨域错误
- [ ] 浏览器控制台无资源404错误

## 五、常见问题及解决方案

### 5.1 网关配置问题

**问题1：访问新域名时报"resource not found"错误**
- **原因**：网关未注册新域名站点
- **解决方案**：联系网关实施同学，在里约网关中添加新域名站点配置

**问题2：网关站点或API代理仍匹配旧域名host**
- **原因**：网关配置未更新
- **解决方案**：检查网关站点配置，确保host配置为新域名；检查API代理规则是否包含旧域名host匹配条件

### 5.2 网络连通性问题

**问题3：新域名访问提示网络不通或超时**
- **原因**：网络策略未配置或防火墙限制
- **解决方案**：联系运维协助排查源、目标网络策略，确保网络连通性

### 5.3 环境配置问题

**问题4：预发布环境访问新域名失败**
- **原因**：预发布环境未部署服务或配置未更新
- **解决方案**：检查预发布环境是否已部署最新版本，确认配置文件中的域名已更新

### 5.4 跨域问题

**问题5：浏览器控制台报跨域错误（CORS Error）**
- **原因**：应用前端代码写死了旧域名，导致跨域访问
- **影响场景**：
  - 前端API请求地址硬编码旧域名
  - 静态资源（JS/CSS）引用硬编码旧域名
  - WebSocket连接地址硬编码旧域名
- **解决方案**：按照本文档"三、技术整改要求"章节进行改造，将所有硬编码域名改为相对路径或动态获取
- **目标**：全站资源和接口使用新域名访问，无跨域报错

**问题6：特定资源或接口跨域，其他正常**
- **原因**：部分URL写死了旧域名地址
- **排查方法**：
  1. 打开浏览器开发者工具（F12）
  2. 查看Network标签，找到报错的请求
  3. 检查请求URL是否包含旧域名
  4. 在代码中搜索该URL，修改为相对路径
- **解决方案**：合作商结合具体报错情况自行定位和修复

### 5.5 认证授权问题

**问题7：登录后提示token无效或未授权**
- **原因**：Cookie域配置问题或token传递时使用了旧域名
- **解决方案**：
  - 检查Cookie的domain配置是否正确
  - 确认SDK的loginUrl已更新
  - 清除浏览器缓存和Cookie后重新测试

**问题8：单点登录失败或反复跳转登录页**
- **原因**：登录回调地址配置了旧域名
- **解决方案**：检查登录配置中的回调地址参数，确保使用相对路径或新域名

### 5.6 资源加载问题

**问题9：页面样式丢失或功能异常**
- **原因**：静态资源（CSS/JS）加载失败，可能路径写死了旧域名
- **排查方法**：查看浏览器控制台Network标签，找到404或跨域失败的资源
- **解决方案**：修改资源引用路径为相对路径

**问题10：iframe嵌套页面加载失败**
- **原因**：iframe的src属性硬编码了旧域名
- **解决方案**：将iframe的src改为相对路径或动态拼接当前域名

### 5.7 第三方服务调用问题

**问题11：后端调用平台Auth服务失败**
- **原因**：后端代码或配置文件中硬编码了旧域名
- **排查方法**：检查配置文件和代码中的平台服务地址
- **解决方案**：参考本文档"3.3 后端整改指引"进行修改

**问题12：回调通知接收不到**
- **原因**：注册的回调地址为旧域名
- **解决方案**：重新注册回调地址，使用新域名

### 5.8 缓存问题

**问题13：明明已修改代码，但浏览器仍访问旧域名**
- **原因**：浏览器或CDN缓存未清除
- **解决方案**：
  - 清除浏览器缓存（Ctrl+Shift+Delete）
  - 使用无痕模式测试
  - 强制刷新页面（Ctrl+F5）
  - 如使用CDN，需清除CDN缓存

**问题14：配置已更新但服务仍使用旧配置**
- **原因**：应用未重启或配置未生效
- **解决方案**：重启应用服务，确保新配置加载

### 5.9 排查工具和方法

**建议排查步骤：**
1. 打开浏览器开发者工具（F12）
2. 切换到Network标签
3. 刷新页面，观察所有请求
4. 筛选出包含旧域名的请求
5. 根据请求类型（XHR、JS、CSS等）定位到具体代码位置
6. 按照本文档指引进行整改

**常用排查命令：**
```bash
# 在项目中搜索旧域名引用
grep -r "fzh.ywtg.foshan.gov.cn" ./src

# 在配置文件中搜索旧域名
grep -r "fzh.ywtg.foshan.gov.cn" ./config
```

## 六、时间节点和责任

### 6.1 整改时间要求
- 各合作商收到通知后，需在 **[X个工作日]** 内完成自查和整改
- 在 **[具体日期]** 前完成预发布环境验证
- 在 **[具体日期]** 完成生产环境切换

### 6.2 责任划分
- **平台方**：负责网关配置、域名切换、技术支持
- **合作商**：负责自有应用的代码整改、测试验证
- **运维方**：负责网络策略配置、环境部署

## 七、技术支持

如在整改过程中遇到问题，请联系平台技术团队：
- **技术支持**：[联系方式]
- **网关问题**：联系网关实施同学
- **网络问题**：联系运维团队

## 八、附录

### 8.1 域名对比表

| 项目 | 旧域名 | 新域名 |
|-----|--------|--------|
| 主域名 | https://fzh.ywtg.foshan.gov.cn | https://yzhcs.fszsj.fs.gdgov.cn |
| 登录服务 | https://fzh.ywtg.foshan.gov.cn/auth/login | https://yzhcs.fszsj.fs.gdgov.cn/auth/login |
| Auth服务 | https://fzh.ywtg.foshan.gov.cn/auth/getUserInfo | https://yzhcs.fszsj.fs.gdgov.cn/auth/getUserInfo |

### 8.2 核心原则总结

✅ **推荐做法：**
- 使用相对路径（如 `/api/user`）
- 使用动态域名获取（如 `window.location.origin`）
- 通过配置文件管理域名
- 使用环境变量管理域名

❌ **禁止做法：**
- 硬编码完整域名地址
- 在代码中直接写死域名
- 多处分散配置域名

---

**文档版本**：v1.0  
**更新日期**：2025-10-19  
**维护部门**：平台技术部
