## 外包前端（中级偏上）面试题｜5 套完整题（每套 10 题，口语化+答案+紧邻追问）

### 套题一
1) 我们先聊响应式：在 Vue 3 里你一般怎么区分用 `ref` 还是 `reactive`？哪些地方容易“丢响应式”，你会怎么避免？ — 答案要点：基础类型用 `ref`，对象用 `reactive`；解构/闭包/跨组件传递/从 `setup` 返回时的解包规则是丢失高发点，用 `toRef/toRefs`、保持引用或包一层来避免；大型不可变对象用 `shallowRef` 降低依赖追踪；`markRaw/readonly` 控制边界。 — 深入追问：Map/Set/数组等集合在响应式里的差异和注意点？什么时候会选择 `shallowReactive` 而不是 `reactive`？
2) 计算与侦听：`computed`、`watch`、`watchEffect` 你分别在什么场景用？ — 答案要点：`computed` 纯派生且有缓存；`watch` 观察明确源（支持 `deep/immediate/flush`）；`watchEffect` 自动依赖收集适合快速副作用；避免循环依赖和多余触发可配合节流/去抖与 `flush: post`。 — 深入追问：如何定位一次输入导致多次级联触发的根因？
3) 自定义组件的双向绑定：怎么实现自定义 `v-model`，以及如何同时支持多个 `v-model` 和修饰符？ — 答案要点：`modelValue`/`update:modelValue` 契约；多模型用 `v-model:checked` 等命名；修饰符通过 `defineModel` 或 props 透传处理；受控/非受控切换时要防止状态撕裂。 — 深入追问：IME 输入法下触发时机如何避免卡顿与错位？
4) 路由与数据：Vue Router 4 里你如何做动态路由、导航守卫、数据预取和滚动还原？ — 答案要点：按需懒加载路由；在 `beforeEach`/路由元信息里做鉴权；数据预取配合取消/去重；`scrollBehavior` 管理回到顶部/记忆滚动；并发导航需处理竞态。 — 深入追问：SSR 里如何避免路由数据在水合前后“双拉一次”？
5) 状态管理：什么时候用 Pinia，什么时候只用 `provide/inject`？登录态/设置类状态如何持久化并跨标签同步？ — 答案要点：跨页面/跨模块共享与可观察需要用 Pinia；单组件树内部可用 `provide/inject` 解耦；持久化用插件（localStorage/IndexedDB），跨页用 `storage` 事件或 BroadcastChannel；SSR 需作用域隔离。 — 深入追问：多标签页同时写入的冲突你怎么合并？
6) 请求层与错误处理：你会怎么封装 axios/fetch？ — 答案要点：统一 baseURL/拦截器；超时/取消（AbortController）；重试+指数退避+抖动；请求去重；鉴权与刷新 Token 并发锁；错误码到用户提示的映射与兜底。 — 深入追问：乐观更新失败如何回滚并保持幂等？
7) 性能与体验：长列表、频繁交互、页面切换你怎么优化？ — 答案要点：窗口化/虚拟滚动；组件拆分与稳定 key；`<KeepAlive>`/`Teleport`/`Suspense` 适配边界；用 Performance/Profiler 定位热点并关注 LCP/INP/CLS。 — 深入追问：如何量化“是否真的优化到了用户感知层”？
8) 构建与分包（Vite）：你如何做分包和长效缓存？ — 答案要点：路由/页面级动态导入；共享依赖抽离；`contenthash+immutable`；入口短缓存；可视化分析定位大模块。 — 深入追问：拆包过度会带来什么副作用，怎么平衡？
9) 安全与跨域：`v-html` 的风险怎么控？CORS 带凭证如何配置？ — 答案要点：防 XSS（模板转义、内容净化、CSP）；CORS 需预检与 `credentials`、`Access-Control-*` 协同；`SameSite`/CSRF 防护。 — 深入追问：第三方富文本你会如何运行时净化？
10) 监控与质量：你会上哪些前端监控？如何把指标纳入工程流程？ — 答案要点：RUM（LCP/INP/CLS）、错误上报去重、Source Map 管理、采样与隐私；在 CI 里设性能预算和报警阈值。 — 深入追问：一次回归如何避免“误报/漏报”？

### 套题二
1) Vue 3 响应式边界：为什么有时候解构 `props` 会不更新？ — 答案要点：解构会断开响应式引用；用 `toRef/toRefs` 或保持整体引用；`readonly` 的只读视图要注意不可直接写。 — 深入追问：`shallowRef` 包裹大型数据带来什么收益与代价？
2) 侦听策略：`watch` 深度侦听和浅侦听如何权衡？ — 答案要点：深侦听成本高，优先侦听具体字段或派生；必要时加节流/去抖；`flush: post` 避免布局抖动。 — 深入追问：如何用调试日志或 `onTrigger`（dev）定位多余触发？
3) 多 `v-model` 一致性：同一组件既要改文本又要改勾选，怎么避免事件竞态？ — 答案要点：模型命名隔离（`v-model:value`/`v-model:checked`），事件节流/合并，单一事实来源（内部先合并再发出更新）。 — 深入追问：受控/非受控切换时如何给出告警并保持 UI 与值一致？
4) 路由权限与并发导航：登录态快过期时，多个并发跳转你怎么处理？ — 答案要点：队列化或幂等校验；刷新 Token 单航道；失败统一跳回登录并带回原路径。 — 深入追问：路由守卫里如何安全地做异步且可取消？
5) Pinia 与 Server State：接口数据缓存你会用 store 还是“请求库”（如 vue-query/SWR 思路）？ — 答案要点：服务端状态适合请求库（失效/重试/去重），本地业务状态适合 store；两者解耦更清晰。 — 深入追问：失效策略如何设计以避免过时数据？
6) 表单校验：异步唯一性校验、依赖字段联动怎么做？ — 答案要点：建立字段依赖图；对输入做去抖与取消；汇总错误到统一提示；提交态与禁用态一致。 — 深入追问：长表单如何做“分片渲染”提升流畅度？
7) 图片与字体：你会如何落地图片与字体优化？ — 答案要点：AVIF/WebP、自适应图与优先级、预加载关键资源；字体子集化与 `font-display`。 — 深入追问：如何验证优化对 LCP 的真实收益？
8) CORS/凭证：跨域要带 Cookie，前后端各怎么改？ — 答案要点：后端允许源、允许方法/头、`Allow-Credentials=true`；前端 `credentials: include` 并注意 `SameSite=None; Secure`。 — 深入追问：预检失败常见原因有哪些？
9) WebSocket 可靠性：弱网下你如何做断线重连？ — 答案要点：指数退避+抖动；心跳/超时；消息 ack/序列号/去重缓存；离线队列；幂等键。 — 深入追问：浏览器休眠/标签页隐藏如何处理心跳策略？
10) 监控与告警：Source Map 泄露和错误上报“去重”怎么做？ — 答案要点：上传到受控服务端、禁公开下载；错误按 message+stack+行列号指纹去重，采样与隐私脱敏。 — 深入追问：如何把关键错误转化为“阻断发版”的门槛？

### 套题三
1) SSR/SSG 选型：营销页、后台、内容站点你怎么选 SSR/SSG/SPA？ — 答案要点：营销页重首屏与 SEO（SSR/SSG）；后台偏交互（SPA/局部 SSR）；内容站点可 ISR/边缘缓存；权衡 TTFB/TTI 与运维复杂度。 — 深入追问：边缘缓存如何兼顾新鲜度与成本？
2) 水合不匹配：只在生产报水合警告你怎么排查？ — 答案要点：找非确定性渲染（时间/随机/异步）；`onServerPrefetch`；对比 server/client 渲染；兜底禁用某些动态片段。 — 深入追问：如何建立回归保护避免再次发生？
3) 构建与拆包：如何决定动态导入的边界？ — 答案要点：路由/页面级、重依赖边界；共享依赖抽离；可视化分析；避免“碎片化请求”。 — 深入追问：如何用性能预算和缓存命中率来校正拆包策略？
4) CSS 与设计系统：BEM、CSS Modules、原子化（Tailwind）你怎么取舍？ — 答案要点：团队习惯/主题化/可维护性；Token 化+组件层复用；按需引入与摇树。 — 深入追问：多品牌主题如何避免样式覆盖地狱？
5) 可访问性（a11y）：表单/弹窗/键盘可达你怎么做？ — 答案要点：语义标签、标签关联、焦点管理、ARIA、陷阱焦点处理、色彩对比。 — 深入追问：虚拟化列表里如何保证焦点正确移动？
6) 列表与数据：10 万行表格如何兼顾编辑与性能？ — 答案要点：窗口化+粘性列；可变行高测量；行内编辑的状态隔离；服务端分页/排序权衡。 — 深入追问：滚动同步在多区域联动时如何实现？
7) 请求层治理：如何避免“重试风暴”？ — 答案要点：退避+抖动、上限、熔断、隔离；区分幂等与非幂等；集中调度。 — 深入追问：全站大面积失败时如何降级与告警？
8) TypeScript 实践：API 类型建模与运行时校验你怎么做？ — 答案要点：可辨识联合、条件类型、最小可信输入；运行时用 zod 等校验，类型与运行时一致。 — 深入追问：如何避免“类型正确但运行时报错”？
9) Core Web Vitals：LCP/INP/CLS 你分别怎么优化？ — 答案要点：LCP 关注关键图/字形和内联关键 CSS；INP 切片任务、避免长任务；CLS 尺寸保留、避免异步插入抖动。 — 深入追问：如何用 RUM 验证是否达标？
10) 版本与回滚：长缓存下怎么“秒回滚”？ — 答案要点：入口短缓存+资源长缓存+版本清单；CDN 回滚；一致性校验。 — 深入追问：如何让用户几乎无感知？

### 套题四
1) Vue 3 组件设计：可复用表单项你会怎么抽象？ — 答案要点：受控/非受控模式、`props`/`emits` 契约、`slots` 与 attrs 透传、校验与错误态统一。 — 深入追问：如何让它在虚拟化列表里也稳定？
2) 事件循环与渲染：为什么 microtask 饥饿会卡帧？ — 答案要点：连续微任务阻塞渲染阶段；需把部分工作移到 macro 或切片到 rAF。 — 深入追问：一个真实案例你会怎么改造？
3) 路由数据预取：你如何避免重复请求与竞态？ — 答案要点：请求去重、取消控制、幂等等；统一错误边界。 — 深入追问：预取失败时的 UI 策略？
4) Pinia 与权限：菜单/按钮权限你怎么落到前端？ — 答案要点：后端下发权限点，前端 store 归一，路由与组件基于权限渲染；防止前端越权需后端再校验。 — 深入追问：权限变更的热更新如何做？
5) 表单体验：异步校验与提交节流怎么权衡？ — 答案要点：输入时去抖校验、提交前集中校验、取消过时请求、禁用提交态。 — 深入追问：移动端输入法兼容？
6) 图像/字体与首屏：你会怎么安排优先级？ — 答案要点：关键图像 preload/priority、延迟非关键；字体子集与 `font-display: swap/fallback`。 — 深入追问：何时该 `preconnect` 第三方域名？
7) 安全：`v-html` 不可避免时你会怎样最小化风险？ — 答案要点：严格白名单净化、隔离样式、CSP、限制事件/脚本；追踪来源与审计。 — 深入追问：富文本里的 iframe/脚本如何处理？
8) 监控：如何把前端错误做成“可行动”的告警？ — 答案要点：分级、阈值、频率去重、关联上下文（用户、路由、版本）；联动回滚。 — 深入追问：灰度和 A/B 下如何区分告警？
9) 构建：多入口与共享依赖冲突怎么解决？ — 答案要点：拆包与 `manualChunks`、externals、锁版本与去重；产物分析。 — 深入追问：哪些库适合 external？
10) WebSocket：断网/休眠恢复后如何补偿？ — 答案要点：从最后 ack 序号恢复、去重缓存、补发队列；显示一致性提示。 — 深入追问：如何避免用户误操作导致重复提交？

### 套题五
1) Vue 3 性能：哪些场景你会用 `<KeepAlive>`，哪些不建议？ — 答案要点：路由级缓存、表单页面返回恢复；副作用过多或数据过旧的场景慎用；配合 include/exclude 控制。 — 深入追问：缓存淘汰策略怎么定？
2) 复杂联动：三个字段互相影响，你怎么避免循环触发？ — 答案要点：派生数据归并、单向数据流；在 `watch` 里加条件/去抖；必要时用状态机。 — 深入追问：如何写防回环测试？
3) SSR 与 SEO：你如何让营销页既快又稳？ — 答案要点：SSR/SSG、内联关键 CSS、关键资源优先级、边缘缓存；监控 TTFB/LCP。 — 深入追问：多语言站点的 SEO 策略？
4) 表格与导出：大数据导出你怎么设计？ — 答案要点：后端异步导出+轮询/回调；前端进度与重试；防止重复触发。 — 深入追问：失败补偿与幂等键？
5) TS 与安全：接口类型正确但运行时报错你怎么防？ — 答案要点：运行时校验（zod）、narrowing、防御式编程；错误路径可观测。 — 深入追问：如何在边界层统一校验？
6) 网络稳健性：离线/弱网你会做哪些适配？ — 答案要点：缓存/占位/回放；请求退避；离线队列；用户提示。 — 深入追问：如何测试这些场景？
7) 资源与缓存：如何制定版本策略以支持“热回滚”？ — 答案要点：入口短缓存+资源长缓存+manifest；CDN 回滚；一致性校验与灰度。 — 深入追问：回滚后如何清理旧资源？
8) 安全与 CSRF：跨域携带 Cookie 时如何防止 CSRF？ — 答案要点：SameSite、CSRF Token、双重提交、后端校验；CORS 严格白名单。 — 深入追问：单页应用路由对 CSRF 有何影响？
9) 监控与性能预算：如何把指标接入 CI？ — 答案要点：构建后 Lighthouse/Bundle 分析；阈值守护，未达标阻断发版。 — 深入追问：如何避免因网络波动误伤？
10) 团队落地：你如何把这些工程实践标准化？ — 答案要点：脚手架模板、lint/commit 规范、CI 检查、文档/示例、培训与 Code Review 清单。 — 深入追问：如何衡量改进成效？

