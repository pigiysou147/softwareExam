## 外包前端（中级偏上）面试题｜5 套完整题（每套 10 题，口语化+答案+紧邻追问）

### 套题一
1) 我们先聊响应式：在 Vue 3 里你一般怎么区分用 `ref` 还是 `reactive`？哪些地方容易“丢响应式”，你会怎么避免？ — 答案（更详细）：
   - 选择：基础/原始类型优先 `ref`（有 `.value`），对象/数组/Map/Set 用 `reactive`（Proxy 包裹）。
   - 丢失场景与修复：
     1) 解构 props 或 reactive 导致引用丢失 → 用 `toRef/toRefs` 或保留整体引用。
     2) 闭包早期捕获旧值 → 在使用点读取最新 `.value` 或传入 getter 函数。
     3) 跨组件传递把 `ref` 解包成原值 → 传 `ref` 本体或用 `v-model` 契约。
     4) 从 `setup` 返回时的解包差异 → 模板会自动解包，JS 中需显式 `.value`。
   - 性能与边界：大对象或第三方不可变数据可用 `shallowRef/shallowReactive`；对无需跟踪的对象用 `markRaw`；读层用 `readonly` 防止误写。
   - 集合：Vue 对数组/Map/Set 的变更方法做了代理触发；注意对嵌套成员的变更需要保持引用一致性。
   — 深入追问：Map/Set 作为键值时比较规则、迭代顺序与依赖收集的影响？什么时候 `shallowReactive` 能显著降低追踪成本？
2) 计算与侦听：`computed`、`watch`、`watchEffect` 你分别在什么场景用？ — 答案（更详细）：
   - `computed`：纯派生值、可缓存、lazy 评估；避免副作用，适合昂贵计算与模板使用。
   - `watch`：对特定源（getter/数组）执行副作用；`deep` 慎用，尽量侦听扁平字段；`flush: post` 适合读 DOM；`immediate` 用于初始同步。
   - `watchEffect`：自动依赖收集，适合快速建立副作用；在回调里返回清理函数处理订阅/定时器。
   - 去抖/节流/合并：在高频输入场景搭配 `debounce/throttle`，或在回调内部批处理；避免循环可通过守卫条件与拆分依赖。
   — 深入追问：如何用 `onTrace`（或日志）标记依赖链路，定位一次输入触发多次渲染的源头？
3) 自定义组件的双向绑定：怎么实现自定义 `v-model`，以及如何同时支持多个 `v-model` 和修饰符？ — 答案（更详细）：
   - 单模型：`props: modelValue` + `emits: update:modelValue`；内部只读外部值，变更通过事件上抛。
   - 多模型：声明多个 prop/emit（如 `value/checked`）；父组件用 `v-model:value`/`v-model:checked`。
   - 修饰符：通过 `defineModel({ set(e){...} })` 或在 props 传入修饰符对象，内部解析并处理 `trim/number/lazy` 等。
   - 受控与非受控：保持单一事实来源；受控优先，非受控提供初始值与内部状态；切换时发出 warning 并同步 UI。
   — 深入追问：IME 输入法下采用 `compositionstart/end` 协调更新，避免频繁上抛与光标错位的策略？
4) 路由与数据：Vue Router 4 里你如何做动态路由、导航守卫、数据预取和滚动还原？ — 答案（更详细）：
   - 动态路由懒加载与权限元信息；全局/路由/组件守卫分层。
   - 数据预取：进入前触发请求，支持取消（AbortController）、请求去重、错误边界。
   - 滚动：`scrollBehavior` 结合历史记录与锚点；对长列表恢复位置。
   - 并发导航：对竞态做序列化或只保留最后一次；在守卫里使用可取消的异步。
   — 深入追问：SSR 合流：`onServerPrefetch` 预取并注水到 state，客户端水合时避免重复请求的实践？
5) 状态管理：什么时候用 Pinia，什么时候只用 `provide/inject`？登录态/设置类状态如何持久化并跨标签同步？ — 答案（更详细）：
   - 选择：应用级共享/可观察/调试需求 → Pinia；局部树的依赖注入/解耦 → provide/inject。
   - 持久化：选择粒度（白名单字段）、序列化策略、版本迁移；IndexedDB 存大对象。
   - 跨页同步：`storage` 事件/BroadcastChannel；冲突合并策略（时间戳/版本号）。
   - SSR 隔离：每请求新实例；避免全局单例泄漏。
   — 深入追问：多标签页竞争写入时如何做幂等与最终一致？
6) 请求层与错误处理：你会怎么封装 axios/fetch？ — 答案（更详细）：
   - 能力：超时/取消、自动重试（退避+抖动+上限）、请求去重、节流、缓存。
   - 鉴权：刷新 Token 单航道锁、失败回退登录、幂等重放。
   - 错误治理：统一码表→用户提示；可重试与兜底；日志与上报。
   - 类型与数据：运行时校验（zod）保护边界，避免“类型正确运行时报错”。
   — 深入追问：乐观更新回滚的一致性与幂等键设计？
7) 性能与体验：长列表、频繁交互、页面切换你怎么优化？ — 答案（更详细）：
   - 列表：窗口化、分片渲染、稳定 key；热点计算移到 computed 或 worker。
   - 交互：输入节流/去抖；合成层（transform/opacity）；避免强制同步布局。
   - 页面：`<KeepAlive>` 恰当缓存；路由懒加载；预取关键资源。
   - 验证：RUM 采集 LCP/INP/CLS；Profiler 对比前后帧时间与长任务数。
   — 深入追问：用户感知阈值（如 100ms/INP）如何转化为验收标准？
8) 构建与分包（Vite）：你如何做分包和长效缓存？ — 答案（更详细）：
   - 边界：按路由/页面与大依赖切割；manualChunks/预构建。
   - 缓存：产物 `contenthash+immutable`；入口 HTML/入口脚本短缓存；manifest 控制回滚。
   - 观测：可视化/请求数与缓存命中评估；避免碎片化请求。
   — 深入追问：如何根据缓存命中率指标回调整分包策略？
9) 安全与跨域：`v-html` 的风险怎么控？CORS 带凭证如何配置？ — 答案（更详细）：
   - XSS：模板转义默认安全；对 `v-html` 进行白名单净化（DOMPurify 类库）、CSP 限制、剥离事件与脚本；来源审计与沙箱策略。
   - CORS：后端严格白名单、允许头与方法、允许凭证；前端 `credentials: include`，Cookie `SameSite=None; Secure`；预检失败排查。
   - CSRF：Token/双重提交；关键操作后端再校验。
   — 深入追问：富文本 iframe/脚本如何处理隔离与净化？
10) 监控与质量：你会上哪些前端监控？如何把指标纳入工程流程？ — 答案（更详细）：
   - 性能：RUM 采集 LCP/INP/CLS、长任务、资源失败；回放抽样。
   - 错误：指纹去重（message+stack+行列号）；Source Map 受控上传；隐私脱敏。
   - 工程：CI 性能预算、打包体积分界、未达标阻断；发布灰度与回滚联动。
   — 深入追问：如何用基线对比和采样策略减少误报/漏报？

### 套题二
1) Vue 3 响应式边界：为什么有时候解构 `props` 会不更新？ — 答案（更详细）：
   - 原因：解构会把响应式引用打平为普通变量，后续变更无法追踪。
   - 方案：用 `toRef/toRefs` 保持引用；模板内用解构需谨慎；`readonly` 用于读层避免误写。
   - 性能：大型 props 可配合 `shallowRef`/`markRaw` 减少追踪；但更新粒度变粗。
   — 深入追问：`shallowRef` 包裹大型数据带来的收益与代价如何权衡？
2) 侦听策略：`watch` 深度侦听和浅侦听如何权衡？ — 答案（更详细）：
   - 优先侦听最小变更源（如 `() => state.a.b`）而非 `deep: true`；对集合可侦听 size 或版本号。
   - 高频变更配合节流/去抖；DOM 相关副作用用 `flush: post`。
   - Dev 排查：在回调与 getter 打日志；用 `onScopeDispose` 清理避免内存泄漏。
   — 深入追问：如何用 `effectScope` 隔离并统一销毁一组侦听？
3) 多 `v-model` 一致性：同一组件既要改文本又要改勾选，怎么避免事件竞态？ — 答案（更详细）：
   - 模型隔离：不同 prop/emit；内部先更新本地草稿，再批量上抛，确保顺序一致。
   - 事件竞态：在同帧合并（microtask 队列）或加节流；对外只暴露一次稳定更新。
   - 受控切换：检测从受控到非受控（或反向）时 warning 并同步初始值。
   — 深入追问：如何保证表单验证与 `v-model` 更新的事务一致性？
4) 路由权限与并发导航：登录态快过期时，多个并发跳转你怎么处理？ — 答案（更详细）：
   - 并发控制：只保留最后一次导航；在守卫内部创建可取消的请求；刷新 Token 通过互斥锁单航道处理。
   - 失败回退：携带原目标用于返回；全局错误态统一处理。
   — 深入追问：守卫中取消时机与 UI 提示如何设计？
5) Pinia 与 Server State：接口数据缓存你会用 store 还是“请求库”（如 vue-query/SWR 思路）？ — 答案（更详细）：
   - 边界：Server State（远端、可失效）→ 请求库；Client State（本地业务）→ Pinia。
   - 策略：失效/预取/去重/重试/缓存时间；聚焦窗口与网络变化时的自动刷新。
   — 深入追问：分页与过滤条件变化时缓存键如何设计？
6) 表单校验：异步唯一性校验、依赖字段联动怎么做？ — 答案要点：建立字段依赖图；对输入做去抖与取消；汇总错误到统一提示；提交态与禁用态一致。 — 深入追问：长表单如何做“分片渲染”提升流畅度？
7) 图片与字体：你会如何落地图片与字体优化？ — 答案要点：AVIF/WebP、自适应图与优先级、预加载关键资源；字体子集化与 `font-display`。 — 深入追问：如何验证优化对 LCP 的真实收益？
8) CORS/凭证：跨域要带 Cookie，前后端各怎么改？ — 答案要点：后端允许源、允许方法/头、`Allow-Credentials=true`；前端 `credentials: include` 并注意 `SameSite=None; Secure`。 — 深入追问：预检失败常见原因有哪些？
9) WebSocket 可靠性：弱网下你如何做断线重连？ — 答案要点：指数退避+抖动；心跳/超时；消息 ack/序列号/去重缓存；离线队列；幂等键。 — 深入追问：浏览器休眠/标签页隐藏如何处理心跳策略？
10) 监控与告警：Source Map 泄露和错误上报“去重”怎么做？ — 答案要点：上传到受控服务端、禁公开下载；错误按 message+stack+行列号指纹去重，采样与隐私脱敏。 — 深入追问：如何把关键错误转化为“阻断发版”的门槛？

### 套题三
1) SSR/SSG 选型：营销页、后台、内容站点你怎么选 SSR/SSG/SPA？ — 答案（更详细）：
   - 营销页：SSR/SSG 优先，内联关键 CSS、优先级提示，CDN 缓存。
   - 后台：复杂交互与状态，SPA 或局部 SSR；关注 INP 与渲染分片。
   - 内容站：SSG/ISR + 边缘缓存；再验证与增量刷新。
   — 深入追问：边缘缓存键设计（语言/地区/登录态）与新鲜度策略？
2) 水合不匹配：只在生产报水合警告你怎么排查？ — 答案（更详细）：
   - 可疑点：使用时间戳、随机数、API 数据顺序、环境差异（UA/窗口尺寸）。
   - 手段：server/client 快照对比，禁用某段渲染定位，`onServerPrefetch` 合流。
   - 兜底：只在客户端渲染的片段用 `client-only` 策略。
   — 深入追问：如何在 CI 中加“水合一致性”快照测试？
3) 构建与拆包：如何决定动态导入的边界？ — 答案（更详细）：
   - 原则：路由边界、功能子域、重量级依赖；手工 `manualChunks` 与自动分包结合。
   - 评估：请求并发数、缓存命中、TTI 变化；可视化与产物追踪。
   — 深入追问：如何制定可回滚的分包变更计划？
4) CSS 与设计系统：BEM、CSS Modules、原子化（Tailwind）你怎么取舍？ — 答案要点：团队习惯/主题化/可维护性；Token 化+组件层复用；按需引入与摇树。 — 深入追问：多品牌主题如何避免样式覆盖地狱？
5) 可访问性（a11y）：表单/弹窗/键盘可达你怎么做？ — 答案要点：语义标签、标签关联、焦点管理、ARIA、陷阱焦点处理、色彩对比。 — 深入追问：虚拟化列表里如何保证焦点正确移动？
6) 列表与数据：10 万行表格如何兼顾编辑与性能？ — 答案要点：窗口化+粘性列；可变行高测量；行内编辑的状态隔离；服务端分页/排序权衡。 — 深入追问：滚动同步在多区域联动时如何实现？
7) 请求层治理：如何避免“重试风暴”？ — 答案要点：退避+抖动、上限、熔断、隔离；区分幂等与非幂等；集中调度。 — 深入追问：全站大面积失败时如何降级与告警？
8) TypeScript 实践：API 类型建模与运行时校验你怎么做？ — 答案要点：可辨识联合、条件类型、最小可信输入；运行时用 zod 等校验，类型与运行时一致。 — 深入追问：如何避免“类型正确但运行时报错”？
9) Core Web Vitals：LCP/INP/CLS 你分别怎么优化？ — 答案（更详细）：
   - LCP：优先级、预加载关键资源、压缩与自适应图、内联关键 CSS。
   - INP：减少同步工作、任务切片/idle、避免阻塞主线程、交互反馈占位。
   - CLS：尺寸保留位、避免动态插入上方、广告/字体策略。
   — 深入追问：RUM 的分位数阈值（P75）与指标归因如何落地？
10) 版本与回滚：长缓存下怎么“秒回滚”？ — 答案要点：入口短缓存+资源长缓存+版本清单；CDN 回滚；一致性校验。 — 深入追问：如何让用户几乎无感知？

### 套题四
1) Vue 3 组件设计：可复用表单项你会怎么抽象？ — 答案（更详细）：
   - 契约：明确 `props`/`emits`、受控（value+update）与非受控（defaultValue+内部状态）。
   - 复用：`slots`（label/help/extra）与 attrs 透传；样式隔离与主题化。
   - 校验：统一错误态、禁用态、loading 与可访问性（aria-*）。
   - 性能：避免不必要渲染；在虚拟化场景下维持稳定 key 和受控草稿。
   — 深入追问：如何让它在虚拟化列表里也稳定？
2) 事件循环与渲染：为什么 microtask 饥饿会卡帧？ — 答案（更详细）：
   - 原因：连续 microtask（Promise.then）阻塞渲染机会；渲染在 microtask 清空后才进入。
   - 方案：把重活拆到 macro（setTimeout 0/rAF/Idle）或分片处理；在 Vue 中减少同步计算，延后到 `flush: post`。
   — 深入追问：一个真实案例你会怎么改造？
3) 路由数据预取：你如何避免重复请求与竞态？ — 答案（更详细）：
   - 去重：以 URL+参数 为 key 的 in-flight map；
   - 取消：AbortController；
   - 幂等：避免重复提交；
   - 错误边界与重试：给出兜底占位+重试按钮。
   — 深入追问：预取失败时的 UI 策略？
4) Pinia 与权限：菜单/按钮权限你怎么落到前端？ — 答案要点：后端下发权限点，前端 store 归一，路由与组件基于权限渲染；防止前端越权需后端再校验。 — 深入追问：权限变更的热更新如何做？
5) 表单体验：异步校验与提交节流怎么权衡？ — 答案要点：输入时去抖校验、提交前集中校验、取消过时请求、禁用提交态。 — 深入追问：移动端输入法兼容？
6) 图像/字体与首屏：你会怎么安排优先级？ — 答案要点：关键图像 preload/priority、延迟非关键；字体子集与 `font-display: swap/fallback`。 — 深入追问：何时该 `preconnect` 第三方域名？
7) 安全：`v-html` 不可避免时你会怎样最小化风险？ — 答案要点：严格白名单净化、隔离样式、CSP、限制事件/脚本；追踪来源与审计。 — 深入追问：富文本里的 iframe/脚本如何处理？
8) 监控：如何把前端错误做成“可行动”的告警？ — 答案要点：分级、阈值、频率去重、关联上下文（用户、路由、版本）；联动回滚。 — 深入追问：灰度和 A/B 下如何区分告警？
9) 构建：多入口与共享依赖冲突怎么解决？ — 答案要点：拆包与 `manualChunks`、externals、锁版本与去重；产物分析。 — 深入追问：哪些库适合 external？
10) WebSocket：断网/休眠恢复后如何补偿？ — 答案要点：从最后 ack 序号恢复、去重缓存、补发队列；显示一致性提示。 — 深入追问：如何避免用户误操作导致重复提交？

### 套题五
1) Vue 3 性能：哪些场景你会用 `<KeepAlive>`，哪些不建议？ — 答案（更详细）：
   - 适用：路由页/表单页回退恢复；切页频繁、初始化代价大的组件。
   - 不适用：强依赖最新数据、副作用多且与生命周期耦合紧的组件。
   - 策略：`include/exclude` 精准命中；手动 `cache` 管理；与路由 meta 协作。
   — 深入追问：缓存淘汰策略怎么定？
2) 复杂联动：三个字段互相影响，你怎么避免循环触发？ — 答案（更详细）：
   - 归并：把派生逻辑集中在 computed；
   - 守卫：在 `watch` 中比较旧新值、加去抖；
   - 架构：必要时用状态机建模，杜绝隐式回环。
   — 深入追问：如何写防回环测试？
3) SSR 与 SEO：你如何让营销页既快又稳？ — 答案（更详细）：
   - 渲染：SSG/SSR；关键 CSS 内联；优先级/预加载关键资源。
   - 缓存：CDN/边缘缓存；回源与失效策略；AB 灰度。
   - 监控：TTFB/LCP/CLS；错误预算。
   — 深入追问：多语言站点的 SEO 策略？
4) 表格与导出：大数据导出你怎么设计？ — 答案（更详细）：
   - 后端：任务队列/异步导出；
   - 前端：进度轮询/回调、失败重试、去重；
   - 幂等：以筛选条件+用户为幂等键；
   — 深入追问：失败补偿与幂等键？
5) TS 与安全：接口类型正确但运行时报错你怎么防？ — 答案要点：运行时校验（zod）、narrowing、防御式编程；错误路径可观测。 — 深入追问：如何在边界层统一校验？
6) 网络稳健性：离线/弱网你会做哪些适配？ — 答案要点：缓存/占位/回放；请求退避；离线队列；用户提示。 — 深入追问：如何测试这些场景？
7) 资源与缓存：如何制定版本策略以支持“热回滚”？ — 答案要点：入口短缓存+资源长缓存+manifest；CDN 回滚；一致性校验与灰度。 — 深入追问：回滚后如何清理旧资源？
8) 安全与 CSRF：跨域携带 Cookie 时如何防止 CSRF？ — 答案要点：SameSite、CSRF Token、双重提交、后端校验；CORS 严格白名单。 — 深入追问：单页应用路由对 CSRF 有何影响？
9) 监控与性能预算：如何把指标接入 CI？ — 答案要点：构建后 Lighthouse/Bundle 分析；阈值守护，未达标阻断发版。 — 深入追问：如何避免因网络波动误伤？
10) 团队落地：你如何把这些工程实践标准化？ — 答案要点：脚手架模板、lint/commit 规范、CI 检查、文档/示例、培训与 Code Review 清单。 — 深入追问：如何衡量改进成效？

